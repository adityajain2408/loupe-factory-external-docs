{
  "build": {
    "env": {
      "ENABLE_FILE_SYSTEM_API": "1"
    }
  },
  "buildCommand": "pip install --break-system-packages -r requirements.txt && mkdocs build",
  "outputDirectory": "site",
  "framework": null
}
