{
  "build": {
    "env": {
      "ENABLE_FILE_SYSTEM_API": "1"
    }
  },
  "buildCommand": "ENABLE_GIT_PLUGINS=true pip install --break-system-packages -r requirements.txt && ENABLE_GIT_PLUGINS=true mkdocs build",
  "outputDirectory": "site",
  "framework": null
}
