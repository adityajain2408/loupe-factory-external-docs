{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "build": {
    "env": {
      "ENABLE_FILE_SYSTEM_API": "1"
    }
  },
  "installCommand": "python3 -m venv .vercel-venv && . .vercel-venv/bin/activate && python -m pip install --disable-pip-version-check -r requirements.txt",
  "buildCommand": ". .vercel-venv/bin/activate && ENABLE_GIT_PLUGINS=true python -m mkdocs build --strict",
  "outputDirectory": "site",
  "framework": null
}
